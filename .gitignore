# web-ext build/sign output
web-ext-artifacts/

# local tooling
node_modules/
.DS_Store

# Mozilla API credentials for signing (MOZILLA_ISSUER / MOZILLA_SECRET)
.env

# web-ext sign bookkeeping
.amo-upload-uuid
